{
  "mcpServers": {
    "context-crafter": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "context_crafter_mcp.server"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
